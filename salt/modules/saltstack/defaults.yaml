default:
  enabled: False
  vscode:
    extensions:
      korekontrol.saltstack:
        enabled: True
        version: null
        settings:
          dash.languageIdToDocsetMap.sls:
            - salt
      # warpnet.salt-lint:
      #   enabled: True
      #   version: null
  zeal:
    docsets:
      SaltStack:
        enabled: True
      Jinja:
        enabled: True