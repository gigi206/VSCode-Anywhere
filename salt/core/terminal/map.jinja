{%- import_yaml 'salt/core/terminal/defaults.yaml' as defaults %}
{%- import_yaml 'salt/core/terminal/profilemap.yaml' as profilemap %}


{%- set terminal = salt['grains.filter_by'](defaults, grain='kernel', base='default',
  merge=salt['grains.filter_by'](profilemap, grain='vscode-anywhere:profile',
    merge=salt['pillar.get']('vscode-anywhere:terminal_core', {})
  )
) %}